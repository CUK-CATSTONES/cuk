enum Status {
  success,
  userNotFound,
  wrongPW,
  error,
}

enum Validate {
  pass,
  error,
  containSpace,
  maxLength,
  minLength,
  notContainNumber,
  containSpecialChar,
  notMatchPW,
  uncompliteWord,
}
